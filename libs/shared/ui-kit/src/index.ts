// @vultus/shared/ui-kit — truly shared Ionic atoms + Stitch design-system theming
// (PLAN §2, §3). Placeholder barrel; shared components arrive when the first UI
// slice needs them. Exported constant keeps the barrel non-empty and lint-clean.
export const SHARED_UI_KIT_PLACEHOLDER = true;
