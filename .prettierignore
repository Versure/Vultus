# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing
.angular

# Generated by tools/scripts/gen-spec-status.mjs — its exact byte output is the
# source of truth for the doc-integrity freshness guard (spec 0058). Prettier
# must not reformat it, or the committed ledger would diverge from the generator
# output and the guard would go red.
/docs/specs/STATUS.md

# pnpm's own writer, not prettier's, owns this format — some existing dependency
# chains here exceed the YAML "simple key" length prettier's parser enforces,
# which crashes the whole file (not just those lines) rather than reformatting it.
/pnpm-lock.yaml
